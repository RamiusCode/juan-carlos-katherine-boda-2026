---
// DireccionSalon.astro
---

<section class="py-4 px-4 relative">
  <div class="max-w-xl mx-auto text-center relative">
    <!-- TÍTULO RECEPCIÓN -->
    <div class="mb-2" id="titulo-direccion">
      <h2
        class="font-charm text-2xl md:text-3xl text-[#2E5FA3] font-bold tracking-wide"
      >
        Recepción Social
      </h2>
    </div>

    <!-- 🏛️ ICONO CON FLORES EN LOS BORDES -->
    <div class="relative mb-4" id="icono-salon">
      <!-- 🌸 FLORES BORDE IZQUIERDO -->
      <div
        class="absolute left-0 top-1/2 -translate-y-1/2 w-14 pointer-events-none -ml-6 sm:-ml-7 md:-ml-8"
        id="flores-sal-izq"
      >
        <img
          src="/imagenes/FlorLateral.webp"
          alt="Flores decorativas izquierda"
          class="w-full h-auto object-contain"
        />
      </div>

      <!-- ICONO CENTRAL -->
      <div class="flex justify-center">
        <img
          src="/imagenes/recepcion.png"
          alt="Icono de recepción"
          class="w-24 h-24 md:w-28 md:h-28 lg:w-32 lg:h-32 object-contain icono-azul"
        />
      </div>

      <!-- 🌸 FLORES BORDE DERECHO -->
      <div
        class="absolute right-0 top-1/2 -translate-y-1/2 w-14 pointer-events-none -mr-6 sm:-mr-7 md:-mr-8"
        id="flores-sal-der"
      >
        <img
          src="/imagenes/FlorLateral.webp"
          alt="Flores decorativas derecha"
          class="w-full h-auto object-contain scale-x-[-1]"
        />
      </div>
    </div>

    <!-- INFORMACIÓN DE UBICACIÓN -->
    <div class="mb-4" id="info-salon">
      <!-- Nombre del lugar -->
      <h3 class="font-charm text-xl md:text-2xl text-[#1B3A6B] font-bold mb-2">
        Salón de Eventos "Evita"
      </h3>

      <!-- Dirección -->
      <p
        class="font-charm text-xl md:text-xl text-[#2E5FA3] leading-relaxed mb-3"
      >
        El Paso, Municipio de Quillacollo
      </p>

      <!-- Hora -->
      <p class="font-charm text-xl md:text-2xl text-[#2E5FA3] font-bold">
        Hora: 16:30
      </p>
    </div>

    <!-- BOTÓN VER UBICACIÓN -->
    <div class="flex justify-center" id="boton-salon">
      <a
        href="https://maps.app.goo.gl/AkTJ5PqNsrkwhK4o9"
        target="_blank"
        rel="noopener noreferrer"
        class="boton-ubicacion-custom"
      >
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width="20"
          height="20"
          viewBox="0 0 24 24"
          fill="currentColor"
        >
          <path
            d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"
          ></path>
        </svg>
        Ver Ubicación
      </a>
    </div>
  </div>
</section>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  if (typeof window !== "undefined") {
    document.addEventListener("DOMContentLoaded", () => {
      gsap.set("#icono-salon", { opacity: 0, scale: 0.8 });
      gsap.set("#titulo-direccion", { opacity: 0, scale: 0.9 });
      gsap.set("#info-salon", { opacity: 0, scale: 0.9 });
      gsap.set("#boton-salon", { opacity: 0, scale: 0.9 });
      gsap.set("#flores-sal-izq", { opacity: 0, x: -30 });
      gsap.set("#flores-sal-der", { opacity: 0, x: 30 });

      ScrollTrigger.create({
        trigger: "#icono-salon",
        start: "top 85%",
        end: "top 20%",
        onEnter: () => {
          const tl = gsap.timeline();
          tl.to(
            "#icono-salon, #flores-sal-izq, #flores-sal-der, #titulo-direccion, #info-salon, #boton-salon",
            { opacity: 1, duration: 1, ease: "power2.out" },
          )
            .to(
              "#icono-salon",
              { scale: 1, duration: 1, ease: "back.out(1.5)" },
              "<",
            )
            .to(
              "#flores-sal-izq",
              { x: 0, duration: 1, ease: "power2.out" },
              "<",
            )
            .to(
              "#flores-sal-der",
              { x: 0, duration: 1, ease: "power2.out" },
              "<",
            )
            .to(
              "#titulo-direccion, #info-salon, #boton-salon",
              { scale: 1, duration: 1, ease: "back.out(1.2)" },
              "<",
            );
        },
        onLeaveBack: () => {
          const tl = gsap.timeline();
          tl.to(
            "#icono-salon, #flores-sal-izq, #flores-sal-der, #titulo-direccion, #info-salon, #boton-salon",
            { opacity: 0, duration: 0.5, ease: "power2.in" },
          )
            .to(
              "#icono-salon",
              { scale: 0.8, duration: 0.5, ease: "power2.in" },
              "<",
            )
            .to(
              "#flores-sal-izq",
              { x: -30, duration: 0.5, ease: "power2.in" },
              "<",
            )
            .to(
              "#flores-sal-der",
              { x: 30, duration: 0.5, ease: "power2.in" },
              "<",
            )
            .to(
              "#titulo-direccion, #info-salon, #boton-salon",
              { scale: 0.9, duration: 0.5, ease: "power2.in" },
              "<",
            );
        },
      });

      console.log("🎉 Sección de Salón inicializada");
    });
  }
</script>

<style>
  /* Icono con tinte azul marino */
  .icono-azul {
    filter: brightness(0) saturate(100%) invert(18%) sepia(60%) saturate(800%)
      hue-rotate(200deg) brightness(85%) contrast(95%);
    transition:
      filter 0.3s ease,
      transform 0.3s ease;
  }

  /* Botón: azul marino → azul claro en hover */
  .boton-ubicacion-custom {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background-color: #1b3a6b;
    color: white;
    font-family: "Charm", cursive;
    font-size: 1.1rem;
    font-weight: bold;
    padding: 0.75rem 2rem;
    border-radius: 9999px;
    text-decoration: none;
    box-shadow: 0 4px 15px rgba(27, 58, 107, 0.35);
    transition: all 0.3s ease;
    letter-spacing: 0.03em;
  }

  .boton-ubicacion-custom:hover {
    background-color: #2e5fa3;
    transform: translateY(-3px) scale(1.05);
    box-shadow: 0 8px 25px rgba(46, 95, 163, 0.45);
  }

  .boton-ubicacion-custom:active {
    transform: translateY(-1px) scale(1.02);
  }
</style>
