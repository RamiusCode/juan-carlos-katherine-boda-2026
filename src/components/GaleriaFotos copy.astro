---
// Galeria.astro
---



<!-- CONTENIDO -->
<section class="py-6 px-4 relative overflow-hidden" id="seccion-galeria">
 <div class="max-w-7xl sm:max-w-sm md:max-w-xl mx-auto relative z-10">
        <!-- TÍTULO -->
        <div class="text-center mb-20" id="titulo-galeria">
            <!-- Línea superior decorativa -->
            <div class="flex items-center justify-center gap-6 mb-8">
                <div
                    class="w-16 h-px bg-gradient-to-r from-transparent to-[#FF9E7F]/60"
                >
                </div>
                <div class="relative">
                    <div class="w-2 h-2 bg-[#FF9E7F] rounded-full"></div>
                    <div
                        class="absolute inset-0 w-2 h-2 bg-[#FF9E7F] rounded-full animate-ping"
                    >
                    </div>
                </div>
                <div
                    class="w-16 h-px bg-gradient-to-l from-transparent to-[#FF9E7F]/60"
                >
                </div>
            </div>

            <h2
                class="font-charm text-2xl md:text-3xl lg:text-3xl text-[#FF9E7F] font-bold mb-4 tracking-wide relative inline-block"
            >
                GALERÍA DE FOTOS
                <div
                    class="absolute -bottom-2 left-0 right-0 h-1 bg-gradient-to-r from-transparent via-[#FF9E7F]/30 to-transparent"
                >
                </div>
            </h2>

            <p
                class="font-charm text-2xl md:text-3xl text-[#3A5A97] mt-8 max-w-3xl mx-auto leading-relaxed"
            >
                Cada momento capturado es un tesoro eterno
            </p>

            <!-- Ornamento inferior -->
            <div class="flex items-center justify-center gap-6 mt-8">
                <div
                    class="w-16 h-px bg-gradient-to-r from-transparent to-[#FF9E7F]/60"
                >
                </div>
                <div class="relative">
                    <div class="w-2 h-2 bg-[#FF9E7F] rounded-full"></div>
                    <div
                        class="absolute inset-0 w-2 h-2 bg-[#FF9E7F] rounded-full animate-ping"
                    >
                    </div>
                </div>
                <div
                    class="w-16 h-px bg-gradient-to-l from-transparent to-[#FF9E7F]/60"
                >
                </div>
            </div>
        </div>

        <!-- GRID BENTO -->
        <div class="grid grid-cols-12 gap-5 md:gap-7">
            <!-- FOTO 1 - Hero Extra Grande -->
            <div
                class="col-span-12 md:col-span-7 foto-card group cursor-pointer"
                data-index="0"
            >
                <div
                    class="relative overflow-hidden rounded-[2rem] transition-all duration-700 aspect-[16/10] bg-gradient-to-br from-[#FF9E7F]/5 to-transparent border border-[#FF9E7F]/10 hover:border-[#FF9E7F]/30"
                >
                    <img
                        src="/imagenes/Foto1.webp"
                        alt="Momento 1"
                        class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-1000 ease-out"
                    />
                    <div
                        class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent opacity-40 group-hover:opacity-70 transition-opacity duration-500"
                    >
                    </div>
                    <div
                        class="absolute inset-0 bg-gradient-to-br from-[#FF9E7F]/0 via-[#FF9E7F]/10 to-[#FF9E7F]/30 opacity-0 group-hover:opacity-100 transition-opacity duration-700"
                    >
                    </div>
                    <div
                        class="absolute inset-6 border border-white/0 group-hover:border-white/40 rounded-3xl transition-all duration-700 pointer-events-none"
                    >
                    </div>
                    <div
                        class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 opacity-0 group-hover:opacity-100 scale-50 group-hover:scale-100 transition-all duration-500"
                    >
                        <div class="relative">
                            <div
                                class="w-24 h-24 rounded-full bg-white/20 backdrop-blur-2xl flex items-center justify-center shadow-2xl border-2 border-white/40"
                            >
                                <svg
                                    class="w-12 h-12 text-white drop-shadow-2xl"
                                    fill="none"
                                    stroke="currentColor"
                                    viewBox="0 0 24 24"
                                    stroke-linecap="round"
                                >
                                    <circle
                                        cx="10.5"
                                        cy="10.5"
                                        r="6.5"
                                        stroke-width="2.5"></circle>
                                    <path
                                        d="M15.5 15.5L21 21"
                                        stroke-width="2.5"></path>
                                    <path
                                        d="M13 8.5a3.5 3.5 0 00-5 0"
                                        stroke="white"
                                        stroke-width="1.8"
                                        opacity="0.5"></path>
                                </svg>
                            </div>
                            <div
                                class="absolute inset-0 w-24 h-24 rounded-full border-2 border-[#FF9E7F]/60 animate-ping-slow"
                            >
                            </div>
                            <div
                                class="absolute inset-0 w-24 h-24 rounded-full bg-[#FF9E7F]/20 blur-2xl"
                            >
                            </div>
                        </div>
                    </div>
                    <div
                        class="absolute bottom-6 left-6 opacity-0 group-hover:opacity-100 transition-all duration-500 transform translate-y-4 group-hover:translate-y-0"
                    >
                        <div
                            class="bg-white/95 backdrop-blur-xl px-6 py-3 rounded-2xl shadow-xl border border-[#FF9E7F]/20"
                        >
                            <span
                                class="text-[#FF9E7F] font-charm text-lg font-semibold"
                                >Momento Especial 1</span
                            >
                        </div>
                    </div>
                </div>
            </div>

            <!-- FOTO 2 - Mediana Vertical -->
            <div
                class="col-span-6 md:col-span-5 foto-card group cursor-pointer"
                data-index="1"
            >
                <div
                    class="relative overflow-hidden rounded-[2rem] transition-all duration-700 aspect-[3/4] bg-gradient-to-br from-[#FF9E7F]/5 to-transparent border border-[#FF9E7F]/10 hover:border-[#FF9E7F]/30"
                >
                    <img
                        src="/imagenes/Foto2.webp"
                        alt="Momento 2"
                        class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-1000 ease-out"
                    />
                    <div
                        class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent opacity-40 group-hover:opacity-70 transition-opacity duration-500"
                    >
                    </div>
                    <div
                        class="absolute inset-0 bg-gradient-to-br from-[#FF9E7F]/0 via-[#FF9E7F]/10 to-[#FF9E7F]/30 opacity-0 group-hover:opacity-100 transition-opacity duration-700"
                    >
                    </div>
                    <div
                        class="absolute inset-6 border border-white/0 group-hover:border-white/40 rounded-3xl transition-all duration-700 pointer-events-none"
                    >
                    </div>
                    <div
                        class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 opacity-0 group-hover:opacity-100 scale-50 group-hover:scale-100 transition-all duration-500"
                    >
                        <div class="relative">
                            <div
                                class="w-20 h-20 rounded-full bg-white/20 backdrop-blur-2xl flex items-center justify-center shadow-2xl border-2 border-white/40"
                            >
                                <svg
                                    class="w-10 h-10 text-white drop-shadow-2xl"
                                    fill="none"
                                    stroke="currentColor"
                                    viewBox="0 0 24 24"
                                    stroke-linecap="round"
                                >
                                    <circle
                                        cx="10.5"
                                        cy="10.5"
                                        r="6.5"
                                        stroke-width="2.5"></circle>
                                    <path
                                        d="M15.5 15.5L21 21"
                                        stroke-width="2.5"></path>
                                    <path
                                        d="M13 8.5a3.5 3.5 0 00-5 0"
                                        stroke="white"
                                        stroke-width="1.8"
                                        opacity="0.5"></path>
                                </svg>
                            </div>
                            <div
                                class="absolute inset-0 w-20 h-20 rounded-full border-2 border-[#FF9E7F]/60 animate-ping-slow"
                            >
                            </div>
                            <div
                                class="absolute inset-0 w-20 h-20 rounded-full bg-[#FF9E7F]/20 blur-2xl"
                            >
                            </div>
                        </div>
                    </div>
                    <div
                        class="absolute bottom-6 left-6 opacity-0 group-hover:opacity-100 transition-all duration-500 transform translate-y-4 group-hover:translate-y-0"
                    >
                        <div
                            class="bg-white/95 backdrop-blur-xl px-5 py-2.5 rounded-2xl shadow-xl border border-[#FF9E7F]/20"
                        >
                            <span
                                class="text-[#FF9E7F] font-charm text-base font-semibold"
                                >Momento 2</span
                            >
                        </div>
                    </div>
                </div>
            </div>

            <!-- FOTO 3 - Mediana Vertical -->
            <div
                class="col-span-6 md:col-span-5 foto-card group cursor-pointer"
                data-index="2"
            >
                <div
                    class="relative overflow-hidden rounded-[2rem] transition-all duration-700 aspect-[3/4] bg-gradient-to-br from-[#FF9E7F]/5 to-transparent border border-[#FF9E7F]/10 hover:border-[#FF9E7F]/30"
                >
                    <img
                        src="/imagenes/Foto3.webp"
                        alt="Momento 3"
                        class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-1000 ease-out"
                    />
                    <div
                        class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent opacity-40 group-hover:opacity-70 transition-opacity duration-500"
                    >
                    </div>
                    <div
                        class="absolute inset-0 bg-gradient-to-br from-[#FF9E7F]/0 via-[#FF9E7F]/10 to-[#FF9E7F]/30 opacity-0 group-hover:opacity-100 transition-opacity duration-700"
                    >
                    </div>
                    <div
                        class="absolute inset-6 border border-white/0 group-hover:border-white/40 rounded-3xl transition-all duration-700 pointer-events-none"
                    >
                    </div>
                    <div
                        class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 opacity-0 group-hover:opacity-100 scale-50 group-hover:scale-100 transition-all duration-500"
                    >
                        <div class="relative">
                            <div
                                class="w-20 h-20 rounded-full bg-white/20 backdrop-blur-2xl flex items-center justify-center shadow-2xl border-2 border-white/40"
                            >
                                <svg
                                    class="w-10 h-10 text-white drop-shadow-2xl"
                                    fill="none"
                                    stroke="currentColor"
                                    viewBox="0 0 24 24"
                                    stroke-linecap="round"
                                >
                                    <circle
                                        cx="10.5"
                                        cy="10.5"
                                        r="6.5"
                                        stroke-width="2.5"></circle>
                                    <path
                                        d="M15.5 15.5L21 21"
                                        stroke-width="2.5"></path>
                                    <path
                                        d="M13 8.5a3.5 3.5 0 00-5 0"
                                        stroke="white"
                                        stroke-width="1.8"
                                        opacity="0.5"></path>
                                </svg>
                            </div>
                            <div
                                class="absolute inset-0 w-20 h-20 rounded-full border-2 border-[#FF9E7F]/60 animate-ping-slow"
                            >
                            </div>
                            <div
                                class="absolute inset-0 w-20 h-20 rounded-full bg-[#FF9E7F]/20 blur-2xl"
                            >
                            </div>
                        </div>
                    </div>
                    <div
                        class="absolute bottom-6 left-6 opacity-0 group-hover:opacity-100 transition-all duration-500 transform translate-y-4 group-hover:translate-y-0"
                    >
                        <div
                            class="bg-white/95 backdrop-blur-xl px-5 py-2.5 rounded-2xl shadow-xl border border-[#FF9E7F]/20"
                        >
                            <span
                                class="text-[#FF9E7F] font-charm text-base font-semibold"
                                >Momento 3</span
                            >
                        </div>
                    </div>
                </div>
            </div>

            <!-- FOTO 4 - Grande Horizontal -->
            <div
                class="col-span-12 md:col-span-7 foto-card group cursor-pointer"
                data-index="3"
            >
                <div
                    class="relative overflow-hidden rounded-[2rem] transition-all duration-700 aspect-[16/9] bg-gradient-to-br from-[#FF9E7F]/5 to-transparent border border-[#FF9E7F]/10 hover:border-[#FF9E7F]/30"
                >
                    <img
                        src="/imagenes/Foto4.webp"
                        alt="Momento 4"
                        class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-1000 ease-out"
                    />
                    <div
                        class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent opacity-40 group-hover:opacity-70 transition-opacity duration-500"
                    >
                    </div>
                    <div
                        class="absolute inset-0 bg-gradient-to-br from-[#FF9E7F]/0 via-[#FF9E7F]/10 to-[#FF9E7F]/30 opacity-0 group-hover:opacity-100 transition-opacity duration-700"
                    >
                    </div>
                    <div
                        class="absolute inset-6 border border-white/0 group-hover:border-white/40 rounded-3xl transition-all duration-700 pointer-events-none"
                    >
                    </div>
                    <div
                        class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 opacity-0 group-hover:opacity-100 scale-50 group-hover:scale-100 transition-all duration-500"
                    >
                        <div class="relative">
                            <div
                                class="w-24 h-24 rounded-full bg-white/20 backdrop-blur-2xl flex items-center justify-center shadow-2xl border-2 border-white/40"
                            >
                                <svg
                                    class="w-12 h-12 text-white drop-shadow-2xl"
                                    fill="none"
                                    stroke="currentColor"
                                    viewBox="0 0 24 24"
                                    stroke-linecap="round"
                                >
                                    <circle
                                        cx="10.5"
                                        cy="10.5"
                                        r="6.5"
                                        stroke-width="2.5"></circle>
                                    <path
                                        d="M15.5 15.5L21 21"
                                        stroke-width="2.5"></path>
                                    <path
                                        d="M13 8.5a3.5 3.5 0 00-5 0"
                                        stroke="white"
                                        stroke-width="1.8"
                                        opacity="0.5"></path>
                                </svg>
                            </div>
                            <div
                                class="absolute inset-0 w-24 h-24 rounded-full border-2 border-[#FF9E7F]/60 animate-ping-slow"
                            >
                            </div>
                            <div
                                class="absolute inset-0 w-24 h-24 rounded-full bg-[#FF9E7F]/20 blur-2xl"
                            >
                            </div>
                        </div>
                    </div>
                    <div
                        class="absolute bottom-6 left-6 opacity-0 group-hover:opacity-100 transition-all duration-500 transform translate-y-4 group-hover:translate-y-0"
                    >
                        <div
                            class="bg-white/95 backdrop-blur-xl px-6 py-3 rounded-2xl shadow-xl border border-[#FF9E7F]/20"
                        >
                            <span
                                class="text-[#FF9E7F] font-charm text-lg font-semibold"
                                >Momento Especial 4</span
                            >
                        </div>
                    </div>
                </div>
            </div>

            <!-- FOTO 5 - Cuadrada -->
            <div
                class="col-span-6 md:col-span-6 foto-card group cursor-pointer"
                data-index="4"
            >
                <div
                    class="relative overflow-hidden rounded-[2rem] transition-all duration-700 aspect-square bg-gradient-to-br from-[#FF9E7F]/5 to-transparent border border-[#FF9E7F]/10 hover:border-[#FF9E7F]/30"
                >
                    <img
                        src="/imagenes/Foto5.webp"
                        alt="Momento 5"
                        class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-1000 ease-out"
                    />
                    <div
                        class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent opacity-40 group-hover:opacity-70 transition-opacity duration-500"
                    >
                    </div>
                    <div
                        class="absolute inset-0 bg-gradient-to-br from-[#FF9E7F]/0 via-[#FF9E7F]/10 to-[#FF9E7F]/30 opacity-0 group-hover:opacity-100 transition-opacity duration-700"
                    >
                    </div>
                    <div
                        class="absolute inset-6 border border-white/0 group-hover:border-white/40 rounded-3xl transition-all duration-700 pointer-events-none"
                    >
                    </div>
                    <div
                        class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 opacity-0 group-hover:opacity-100 scale-50 group-hover:scale-100 transition-all duration-500"
                    >
                        <div class="relative">
                            <div
                                class="w-20 h-20 rounded-full bg-white/20 backdrop-blur-2xl flex items-center justify-center shadow-2xl border-2 border-white/40"
                            >
                                <svg
                                    class="w-10 h-10 text-white drop-shadow-2xl"
                                    fill="none"
                                    stroke="currentColor"
                                    viewBox="0 0 24 24"
                                    stroke-linecap="round"
                                >
                                    <circle
                                        cx="10.5"
                                        cy="10.5"
                                        r="6.5"
                                        stroke-width="2.5"></circle>
                                    <path
                                        d="M15.5 15.5L21 21"
                                        stroke-width="2.5"></path>
                                    <path
                                        d="M13 8.5a3.5 3.5 0 00-5 0"
                                        stroke="white"
                                        stroke-width="1.8"
                                        opacity="0.5"></path>
                                </svg>
                            </div>
                            <div
                                class="absolute inset-0 w-20 h-20 rounded-full border-2 border-[#FF9E7F]/60 animate-ping-slow"
                            >
                            </div>
                            <div
                                class="absolute inset-0 w-20 h-20 rounded-full bg-[#FF9E7F]/20 blur-2xl"
                            >
                            </div>
                        </div>
                    </div>
                    <div
                        class="absolute bottom-6 left-6 opacity-0 group-hover:opacity-100 transition-all duration-500 transform translate-y-4 group-hover:translate-y-0"
                    >
                        <div
                            class="bg-white/95 backdrop-blur-xl px-5 py-2.5 rounded-2xl shadow-xl border border-[#FF9E7F]/20"
                        >
                            <span
                                class="text-[#FF9E7F] font-charm text-base font-semibold"
                                >Momento 5</span
                            >
                        </div>
                    </div>
                </div>
            </div>

            <!-- FOTO 6 - Cuadrada -->
            <div
                class="col-span-6 md:col-span-6 foto-card group cursor-pointer"
                data-index="5"
            >
                <div
                    class="relative overflow-hidden rounded-[2rem] transition-all duration-700 aspect-square bg-gradient-to-br from-[#FF9E7F]/5 to-transparent border border-[#FF9E7F]/10 hover:border-[#FF9E7F]/30"
                >
                    <img
                        src="/imagenes/Foto6.webp"
                        alt="Momento 6"
                        class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-1000 ease-out"
                    />
                    <div
                        class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent opacity-40 group-hover:opacity-70 transition-opacity duration-500"
                    >
                    </div>
                    <div
                        class="absolute inset-0 bg-gradient-to-br from-[#FF9E7F]/0 via-[#FF9E7F]/10 to-[#FF9E7F]/30 opacity-0 group-hover:opacity-100 transition-opacity duration-700"
                    >
                    </div>
                    <div
                        class="absolute inset-6 border border-white/0 group-hover:border-white/40 rounded-3xl transition-all duration-700 pointer-events-none"
                    >
                    </div>
                    <div
                        class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 opacity-0 group-hover:opacity-100 scale-50 group-hover:scale-100 transition-all duration-500"
                    >
                        <div class="relative">
                            <div
                                class="w-20 h-20 rounded-full bg-white/20 backdrop-blur-2xl flex items-center justify-center shadow-2xl border-2 border-white/40"
                            >
                                <svg
                                    class="w-10 h-10 text-white drop-shadow-2xl"
                                    fill="none"
                                    stroke="currentColor"
                                    viewBox="0 0 24 24"
                                    stroke-linecap="round"
                                >
                                    <circle
                                        cx="10.5"
                                        cy="10.5"
                                        r="6.5"
                                        stroke-width="2.5"></circle>
                                    <path
                                        d="M15.5 15.5L21 21"
                                        stroke-width="2.5"></path>
                                    <path
                                        d="M13 8.5a3.5 3.5 0 00-5 0"
                                        stroke="white"
                                        stroke-width="1.8"
                                        opacity="0.5"></path>
                                </svg>
                            </div>
                            <div
                                class="absolute inset-0 w-20 h-20 rounded-full border-2 border-[#FF9E7F]/60 animate-ping-slow"
                            >
                            </div>
                            <div
                                class="absolute inset-0 w-20 h-20 rounded-full bg-[#FF9E7F]/20 blur-2xl"
                            >
                            </div>
                        </div>
                    </div>
                    <div
                        class="absolute bottom-6 left-6 opacity-0 group-hover:opacity-100 transition-all duration-500 transform translate-y-4 group-hover:translate-y-0"
                    >
                        <div
                            class="bg-white/95 backdrop-blur-xl px-5 py-2.5 rounded-2xl shadow-xl border border-[#FF9E7F]/20"
                        >
                            <span
                                class="text-[#FF9E7F] font-charm text-base font-semibold"
                                >Momento 6</span
                            >
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- ORNAMENTO INFERIOR -->
        <div class="flex justify-center mt-20" id="ornamento-inferior">
            <div class="flex items-center gap-4">
                <div
                    class="w-24 h-px bg-gradient-to-r from-transparent to-[#FF9E7F]/60"
                >
                </div>
                <div class="relative">
                    <div class="w-2.5 h-2.5 bg-[#FF9E7F] rounded-full"></div>
                    <div
                        class="absolute inset-0 w-2.5 h-2.5 bg-[#FF9E7F] rounded-full animate-ping"
                    >
                    </div>
                </div>
                <div
                    class="w-24 h-px bg-gradient-to-l from-transparent to-[#FF9E7F]/60"
                >
                </div>
            </div>
        </div>
    </div>

    <!-- MODAL FULLSCREEN -->
    <div
        id="modal-galeria"
        class="fixed inset-0 z-50 hidden items-center justify-center"
        style="background: rgba(0,0,0,0.96);"
    >
        <div
            class="absolute inset-0 pointer-events-none overflow-hidden"
            id="modal-particulas"
        >
        </div>

        <button
            id="cerrar-modal"
            class="absolute top-6 right-6 w-12 h-12 rounded-full flex items-center justify-center transition-all duration-500 z-50 hover:rotate-90"
            style="background: rgba(235,204,196,0.15); border: 1px solid rgba(235,204,196,0.3);"
        >
            <svg
                class="w-5 h-5"
                fill="none"
                stroke="#c9968d"
                viewBox="0 0 24 24"
            >
                <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M6 18L18 6M6 6l12 12"></path>
            </svg>
        </button>

        <div
            class="relative w-full h-full flex items-center justify-center px-4 md:px-20"
        >
            <button
                id="btn-anterior"
                class="absolute left-3 md:left-8 z-40 w-12 h-12 rounded-full flex items-center justify-center transition-all duration-300 hover:scale-110"
                style="background: rgba(235,204,196,0.12); border: 1px solid rgba(235,204,196,0.25);"
            >
                <svg
                    class="w-6 h-6"
                    fill="none"
                    stroke="#c9968d"
                    viewBox="0 0 24 24"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M15 19l-7-7 7-7"></path>
                </svg>
            </button>

            <div
                class="relative w-full max-w-xs md:max-w-md z-30"
                id="modal-contenido"
            >
                <div class="absolute -inset-4 pointer-events-none">
                    <div
                        class="absolute top-0 left-0 w-6 h-6 border-t border-l border-[#c9968d]/30"
                    >
                    </div>
                    <div
                        class="absolute top-0 right-0 w-6 h-6 border-t border-r border-[#c9968d]/30"
                    >
                    </div>
                    <div
                        class="absolute bottom-0 left-0 w-6 h-6 border-b border-l border-[#c9968d]/30"
                    >
                    </div>
                    <div
                        class="absolute bottom-0 right-0 w-6 h-6 border-b border-r border-[#c9968d]/30"
                    >
                    </div>
                </div>
                <div id="modal-track" class="flex will-change-transform"></div>
                <div
                    class="absolute -bottom-10 left-1/2 -translate-x-1/2 flex items-center gap-3 whitespace-nowrap"
                >
                    <span
                        id="modal-num"
                        class="font-cathylise text-[#c9968d]/70 text-base"
                        >01</span
                    >
                    <div class="flex gap-1" id="modal-dots-mini"></div>
                    <span
                        class="font-cathylise text-white/30 text-base"
                        id="modal-total">09</span
                    >
                </div>
            </div>

            <button
                id="btn-siguiente"
                class="absolute right-3 md:right-8 z-40 w-12 h-12 rounded-full flex items-center justify-center transition-all duration-300 hover:scale-110"
                style="background: rgba(235,204,196,0.12); border: 1px solid rgba(235,204,196,0.25);"
            >
                <svg
                    class="w-6 h-6"
                    fill="none"
                    stroke="#c9968d"
                    viewBox="0 0 24 24"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M9 5l7 7-7 7"></path>
                </svg>
            </button>
        </div>
    </div>
</section>

<script>
    import { gsap } from "gsap";
    import { ScrollTrigger } from "gsap/ScrollTrigger";
    gsap.registerPlugin(ScrollTrigger);

    if (typeof window !== "undefined") {
        document.addEventListener("DOMContentLoaded", () => {
            gsap.set("#galeria-onda", { opacity: 0, y: -20 });
            gsap.set("#linea-deco", { opacity: 0 });
            gsap.set("#texto-historia", { opacity: 0, y: 20 });
            gsap.set("#flor-galeria", { opacity: 0, x: 30 });
            gsap.set("#galeria-wrapper", { opacity: 0, scale: 0.96 });
            gsap.set("#miniaturas-container", { opacity: 0, y: 10 });
            gsap.set("#btn-galeria-container", { opacity: 0, y: 10 });
            gsap.set("#credito-galeria", { opacity: 0 });

            ScrollTrigger.create({
                trigger: "#seccion-galeria",
                start: "top 85%",
                onEnter: () => {
                    const tl = gsap.timeline();
                    tl.to("#galeria-onda", {
                        opacity: 1,
                        y: 0,
                        duration: 0.8,
                        ease: "power2.out",
                    })
                        .to(
                            "#linea-deco",
                            { opacity: 1, duration: 0.6 },
                            "-=0.3",
                        )
                        .to(
                            "#texto-historia",
                            {
                                opacity: 1,
                                y: 0,
                                duration: 0.8,
                                ease: "power2.out",
                            },
                            "-=0.3",
                        )
                        .to(
                            "#flor-galeria",
                            {
                                opacity: 1,
                                x: 0,
                                duration: 0.7,
                                ease: "power2.out",
                            },
                            "-=0.4",
                        )
                        .to(
                            "#galeria-wrapper",
                            {
                                opacity: 1,
                                scale: 1,
                                duration: 1,
                                ease: "power3.out",
                            },
                            "-=0.3",
                        )
                        .to(
                            "#miniaturas-container",
                            {
                                opacity: 1,
                                y: 0,
                                duration: 0.6,
                                ease: "power2.out",
                            },
                            "-=0.2",
                        )
                        .to(
                            "#btn-galeria-container",
                            {
                                opacity: 1,
                                y: 0,
                                duration: 0.6,
                                ease: "back.out(1.5)",
                            },
                            "-=0.3",
                        )
                        .to(
                            "#credito-galeria",
                            { opacity: 1, duration: 0.6 },
                            "-=0.3",
                        );
                },
                onLeaveBack: () => {
                    gsap.set("#galeria-onda", { opacity: 0, y: -20 });
                    gsap.set("#linea-deco", { opacity: 0 });
                    gsap.set("#texto-historia", { opacity: 0, y: 20 });
                    gsap.set("#flor-galeria", { opacity: 0, x: 30 });
                    gsap.set("#galeria-wrapper", { opacity: 0, scale: 0.96 });
                    gsap.set("#miniaturas-container", { opacity: 0, y: 10 });
                    gsap.set("#btn-galeria-container", { opacity: 0, y: 10 });
                    gsap.set("#credito-galeria", { opacity: 0 });
                },
            });

            const fotos = [
                { src: "/imagenes/fot1.webp", alt: "Nuestra historia 1" },
                { src: "/imagenes/foto2.webp", alt: "Nuestra historia 2" },
                { src: "/imagenes/foto3.webp", alt: "Nuestra historia 3" },
                { src: "/imagenes/fot4.webp", alt: "Nuestra historia 4" },
                { src: "/imagenes/fot5.webp", alt: "Nuestra historia 5" },
                { src: "/imagenes/fot6.webp", alt: "Nuestra historia 6" },
                { src: "/imagenes/foto7.webp", alt: "Nuestra historia 7" },
                { src: "/imagenes/foto8.webp", alt: "Nuestra historia 8" },
                { src: "/imagenes/foto9.webp", alt: "Nuestra historia 9" },
            ];

            const DURACION = 4500;
            const CIRCUNFERENCIA = 94.2;
            const viewport = document.getElementById("slides-viewport");
            const dotsLateral = document.getElementById("dots-lateral");
            const numSlide = document.getElementById("num-slide");
            const progresoCirculo = document.getElementById("progreso-circulo");
            const miniaturasTrack = document.getElementById("miniaturas-track");

            fotos.forEach((foto, i) => {
                if (!viewport) return;
                const slide = document.createElement("div");
                slide.className = "slide-item absolute inset-0 cursor-pointer";
                slide.setAttribute("data-index", i.toString());
                slide.style.cssText = `opacity:${i === 0 ? "1" : "0"}; z-index:${i === 0 ? "2" : "1"};`;
                slide.innerHTML = `
                    <div class="w-full h-full overflow-hidden">
                        <img src="${foto.src}" alt="${foto.alt}"
                            class="slide-img w-full h-full object-cover"
                            style="transform-origin: center center;" />
                    </div>
                    <div class="absolute inset-0"
                        style="background: linear-gradient(135deg, rgba(235,204,196,0.08) 0%, transparent 50%, rgba(99,119,141,0.06) 100%);">
                    </div>
                    <div class="slide-hover-icon absolute inset-0 flex items-center justify-center opacity-0 transition-opacity duration-500">
                        <div class="relative">
                            <div class="w-20 h-20 rounded-full flex items-center justify-center"
                                style="background: rgba(235,204,196,0.2); backdrop-filter: blur(8px); border: 1px solid rgba(235,204,196,0.4);">
                                <svg class="w-8 h-8" fill="none" stroke="#c9968d" viewBox="0 0 24 24">
                                    <circle cx="10.5" cy="10.5" r="6.5" stroke-width="1.5"></circle>
                                    <path d="M15.5 15.5L21 21" stroke-width="1.5"></path>
                                </svg>
                            </div>
                            <div class="absolute inset-0 rounded-full animate-ping"
                                style="border: 1px solid rgba(235,204,196,0.3);"></div>
                        </div>
                    </div>
                `;
                slide.addEventListener("mouseenter", () => {
                    const icon = slide.querySelector(
                        ".slide-hover-icon",
                    ) as HTMLElement;
                    if (icon) icon.style.opacity = "1";
                });
                slide.addEventListener("mouseleave", () => {
                    const icon = slide.querySelector(
                        ".slide-hover-icon",
                    ) as HTMLElement;
                    if (icon) icon.style.opacity = "0";
                });
                slide.addEventListener("click", () => abrirModal(i));
                viewport.appendChild(slide);

                if (dotsLateral) {
                    const dot = document.createElement("div");
                    dot.className =
                        "dot-lat transition-all duration-500 rounded-full";
                    dot.style.cssText = `width:${i === 0 ? "8px" : "4px"}; height:${i === 0 ? "8px" : "4px"}; background:${i === 0 ? "rgba(235,204,196,0.9)" : "rgba(235,204,196,0.3)"}`;
                    dotsLateral.appendChild(dot);
                }

                if (miniaturasTrack) {
                    const mini = document.createElement("button");
                    mini.className =
                        "miniatura-btn flex-shrink-0 snap-start rounded-xl overflow-hidden";
                    mini.setAttribute("data-mini", i.toString());
                    mini.style.cssText = `width:52px; height:52px; border:2px solid ${i === 0 ? "rgba(235,204,196,0.9)" : "rgba(235,204,196,0.2)"}; opacity:${i === 0 ? "1" : "0.6"};`;
                    mini.innerHTML = `<img src="${foto.src}" alt="${foto.alt}" class="w-full h-full object-cover" />`;
                    mini.addEventListener("click", () => {
                        irASlide(i);
                        clearTimeout(autoTimer);
                        programarSiguiente();
                    });
                    miniaturasTrack.appendChild(mini);
                }
            });

            let kbTween: gsap.core.Tween | null = null;
            function iniciarKenBurns(index: number) {
                if (kbTween) kbTween.kill();
                const slides = document.querySelectorAll(".slide-item");
                const img = slides[index]?.querySelector(
                    ".slide-img",
                ) as HTMLElement;
                if (!img) return;
                gsap.set(img, { scale: 1, x: "0%", y: "0%" });
                const dirs = [
                    { scale: 1.08, x: "-2%", y: "-2%" },
                    { scale: 1.08, x: "2%", y: "-1%" },
                    { scale: 1.06, x: "-1%", y: "2%" },
                    { scale: 1.07, x: "1%", y: "-2%" },
                    { scale: 1.06, x: "-2%", y: "1%" },
                    { scale: 1.08, x: "2%", y: "2%" },
                    { scale: 1.07, x: "-1%", y: "-1%" },
                    { scale: 1.06, x: "2%", y: "-2%" },
                    { scale: 1.08, x: "-2%", y: "2%" },
                ];
                const d = dirs[index % dirs.length];
                kbTween = gsap.to(img, {
                    scale: d.scale,
                    x: d.x,
                    y: d.y,
                    duration: DURACION / 1000 + 1,
                    ease: "none",
                });
            }

            let progresoTween: gsap.core.Tween | null = null;
            function iniciarProgreso() {
                if (progresoTween) progresoTween.kill();
                if (!progresoCirculo) return;
                gsap.set(progresoCirculo, { strokeDashoffset: CIRCUNFERENCIA });
                progresoTween = gsap.to(progresoCirculo, {
                    strokeDashoffset: 0,
                    duration: DURACION / 1000,
                    ease: "none",
                });
            }

            let slideActual = 0;
            let autoTimer: any;
            let pausado = false;
            let rafId: number | null = null;

            function actualizarDots(index: number) {
                document.querySelectorAll(".dot-lat").forEach((d, i) => {
                    const el = d as HTMLElement;
                    el.style.width = i === index ? "8px" : "4px";
                    el.style.height = i === index ? "8px" : "4px";
                    el.style.background =
                        i === index
                            ? "rgba(235,204,196,0.9)"
                            : "rgba(235,204,196,0.3)";
                });
                if (numSlide)
                    numSlide.textContent = String(index + 1).padStart(2, "0");

                document.querySelectorAll(".miniatura-btn").forEach((m, i) => {
                    const el = m as HTMLElement;
                    el.style.border =
                        i === index
                            ? "2px solid rgba(235,204,196,0.9)"
                            : "2px solid rgba(235,204,196,0.2)";
                    el.style.opacity = i === index ? "1" : "0.6";
                });

                const miniActiva = document.querySelector(
                    `[data-mini="${index}"]`,
                ) as HTMLElement;
                const track = document.getElementById("miniaturas-track");
                if (miniActiva && track) {
                    const offsetLeft =
                        miniActiva.offsetLeft -
                        track.clientWidth / 2 +
                        miniActiva.clientWidth / 2;
                    track.scrollTo({ left: offsetLeft, behavior: "smooth" });
                }
            }

            function irASlide(nuevoIndex: number) {
                if (rafId !== null) cancelAnimationFrame(rafId);

                const slides = document.querySelectorAll(".slide-item");
                const anterior = slideActual;
                slideActual = (nuevoIndex + fotos.length) % fotos.length;

                const slideAnterior = slides[anterior] as HTMLElement;
                const slideSiguiente = slides[slideActual] as HTMLElement;

                slideAnterior.style.zIndex = "2";
                slideSiguiente.style.zIndex = "3";
                slideSiguiente.style.opacity = "0";

                const duracionMs = 1400;
                let start: number | null = null;

                function frame(ts: number) {
                    if (!start) start = ts;
                    const t = Math.min((ts - start) / duracionMs, 1);
                    const ease =
                        t < 0.5 ? 2 * t * t : 1 - Math.pow(-2 * t + 2, 2) / 2;
                    slideSiguiente.style.opacity = String(ease);
                    slideAnterior.style.opacity = String(1 - ease);

                    if (t < 1) {
                        rafId = requestAnimationFrame(frame);
                    } else {
                        slideSiguiente.style.opacity = "1";
                        slideSiguiente.style.zIndex = "2";
                        slideAnterior.style.opacity = "0";
                        slideAnterior.style.zIndex = "1";
                        rafId = null;
                    }
                }

                rafId = requestAnimationFrame(frame);
                actualizarDots(slideActual);
                iniciarKenBurns(slideActual);
                iniciarProgreso();
            }

            function programarSiguiente() {
                clearTimeout(autoTimer);
                autoTimer = setTimeout(() => {
                    if (!pausado) {
                        irASlide(slideActual + 1);
                        programarSiguiente();
                    }
                }, DURACION);
            }

            const wrapper = document.getElementById("galeria-wrapper");
            wrapper?.addEventListener("mouseenter", () => {
                pausado = true;
                progresoTween?.pause();
                kbTween?.pause();
            });
            wrapper?.addEventListener("mouseleave", () => {
                pausado = false;
                progresoTween?.resume();
                kbTween?.resume();
                programarSiguiente();
            });

            let touchX = 0;
            viewport?.addEventListener(
                "touchstart",
                (e) => {
                    touchX = e.touches[0].clientX;
                },
                { passive: true },
            );
            viewport?.addEventListener("touchend", (e) => {
                const diff = touchX - e.changedTouches[0].clientX;
                if (Math.abs(diff) > 50) {
                    irASlide(diff > 0 ? slideActual + 1 : slideActual - 1);
                    programarSiguiente();
                }
            });

            iniciarKenBurns(0);
            actualizarDots(0);

            // ── INTERSECTION OBSERVER ──
            const observerGaleria = new IntersectionObserver(
                (entries) => {
                    entries.forEach((entry) => {
                        if (entry.isIntersecting) {
                            pausado = false;
                            iniciarProgreso();
                            programarSiguiente();
                        } else {
                            pausado = true;
                            clearTimeout(autoTimer);
                            autoTimer = null;
                            progresoTween?.pause();
                            kbTween?.pause();
                            if (rafId !== null) {
                                cancelAnimationFrame(rafId);
                                rafId = null;
                            }
                        }
                    });
                },
                { threshold: 0, rootMargin: "0px" },
            );

            const galeriaSection = document.getElementById("seccion-galeria");
            if (galeriaSection) observerGaleria.observe(galeriaSection);

            // ── MODAL ──
            const modal = document.getElementById("modal-galeria");
            const modalTrack = document.getElementById("modal-track");
            const modalNum = document.getElementById("modal-num");
            const modalTotal = document.getElementById("modal-total");
            const modalDotsMini = document.getElementById("modal-dots-mini");
            const btnAnterior = document.getElementById("btn-anterior");
            const btnSiguiente = document.getElementById("btn-siguiente");
            const cerrarModalBtn = document.getElementById("cerrar-modal");
            const btnVerGaleria = document.getElementById("btn-ver-galeria");

            if (modalTotal)
                modalTotal.textContent = String(fotos.length).padStart(2, "0");

            fotos.forEach((foto) => {
                if (!modalTrack) return;
                const slide = document.createElement("div");
                slide.className =
                    "min-w-full flex items-center justify-center px-2";
                slide.innerHTML = `
                    <div class="relative w-full rounded-2xl overflow-hidden" style="border:1px solid rgba(235,204,196,0.15);">
                        <img src="${foto.src}" alt="${foto.alt}" class="w-full h-auto max-h-[80vh] object-contain" style="background:rgba(0,0,0,0.3);" />
                    </div>
                `;
                modalTrack.appendChild(slide);
            });

            fotos.forEach((_, i) => {
                if (!modalDotsMini) return;
                const d = document.createElement("div");
                d.className = "modal-dot-mini rounded-full";
                d.style.cssText = `width:${i === 0 ? "16px" : "4px"}; height:4px; background:${i === 0 ? "rgba(235,204,196,0.8)" : "rgba(235,204,196,0.2)"}; transition:all 0.4s;`;
                modalDotsMini.appendChild(d);
            });

            let indiceModal = 0;

            function actualizarModalDots(index: number) {
                document.querySelectorAll(".modal-dot-mini").forEach((d, i) => {
                    const el = d as HTMLElement;
                    el.style.width = i === index ? "16px" : "4px";
                    el.style.background =
                        i === index
                            ? "rgba(235,204,196,0.8)"
                            : "rgba(235,204,196,0.2)";
                });
                if (modalNum)
                    modalNum.textContent = String(index + 1).padStart(2, "0");
            }

            const moverModal = (nuevo: number, animar = true) => {
                if (!modalTrack) return;
                indiceModal = (nuevo + fotos.length) % fotos.length;
                if (animar) {
                    gsap.to(modalTrack, {
                        x: `-${indiceModal * 100}%`,
                        duration: 0.6,
                        ease: "power3.out",
                    });
                } else {
                    gsap.set(modalTrack, { x: `-${indiceModal * 100}%` });
                }
                actualizarModalDots(indiceModal);
            };

            function crearParticulas() {
                const container = document.getElementById("modal-particulas");
                if (!container) return;
                container.innerHTML = "";
                for (let i = 0; i < 12; i++) {
                    const p = document.createElement("div");
                    p.style.cssText = `position:absolute; width:${Math.random() * 3 + 1}px; height:${Math.random() * 3 + 1}px; background:rgba(235,204,196,${Math.random() * 0.3 + 0.1}); border-radius:50%; left:${Math.random() * 100}%; top:${Math.random() * 100}%;`;
                    container.appendChild(p);
                    gsap.to(p, {
                        y: -30 - Math.random() * 40,
                        x: (Math.random() - 0.5) * 20,
                        opacity: 0,
                        duration: 3 + Math.random() * 4,
                        repeat: -1,
                        delay: Math.random() * 3,
                        ease: "none",
                    });
                }
            }

            const abrirModal = (indice: number) => {
                if (!modal) return;
                pausado = true;
                clearTimeout(autoTimer);
                progresoTween?.pause();
                kbTween?.pause();
                modal.classList.remove("hidden");
                modal.classList.add("flex");
                document.body.style.overflow = "hidden";
                moverModal(indice, false);
                crearParticulas();
                gsap.fromTo(
                    modal,
                    { opacity: 0 },
                    { opacity: 1, duration: 0.5, ease: "power2.out" },
                );
                gsap.fromTo(
                    "#modal-contenido",
                    { scale: 0.88, opacity: 0, y: 20 },
                    {
                        scale: 1,
                        opacity: 1,
                        y: 0,
                        duration: 0.7,
                        ease: "power3.out",
                        delay: 0.1,
                    },
                );
            };

            const cerrarModalFn = () => {
                if (!modal) return;
                gsap.to("#modal-contenido", {
                    scale: 0.88,
                    opacity: 0,
                    y: 20,
                    duration: 0.4,
                    ease: "power2.in",
                });
                gsap.to(modal, {
                    opacity: 0,
                    duration: 0.4,
                    ease: "power2.in",
                    delay: 0.15,
                    onComplete: () => {
                        modal.classList.add("hidden");
                        modal.classList.remove("flex");
                        document.body.style.overflow = "";
                        pausado = false;
                        iniciarProgreso();
                        programarSiguiente();
                    },
                });
            };

            btnVerGaleria?.addEventListener("click", () => abrirModal(0));
            btnAnterior?.addEventListener("click", (e) => {
                e.stopPropagation();
                moverModal(indiceModal - 1);
            });
            btnSiguiente?.addEventListener("click", (e) => {
                e.stopPropagation();
                moverModal(indiceModal + 1);
            });
            cerrarModalBtn?.addEventListener("click", (e) => {
                e.stopPropagation();
                cerrarModalFn();
            });
            modal?.addEventListener("click", (e) => {
                if (e.target === modal) cerrarModalFn();
            });

            document.addEventListener("keydown", (e) => {
                if (modal && !modal.classList.contains("hidden")) {
                    if (e.key === "Escape") cerrarModalFn();
                    if (e.key === "ArrowLeft") moverModal(indiceModal - 1);
                    if (e.key === "ArrowRight") moverModal(indiceModal + 1);
                }
            });

            let mTouchX = 0;
            modal?.addEventListener(
                "touchstart",
                (e) => {
                    mTouchX = e.touches[0].clientX;
                },
                { passive: true },
            );
            modal?.addEventListener("touchend", (e) => {
                const diff = mTouchX - e.changedTouches[0].clientX;
                if (Math.abs(diff) > 60)
                    moverModal(diff > 0 ? indiceModal + 1 : indiceModal - 1);
            });
        });
    }
</script>

<style>
    #slides-viewport {
        position: relative;
        contain: strict;
        will-change: transform;
    }
    .slide-item {
        will-change: opacity;
        contain: layout style;
    }
    .slide-img {
        will-change: transform;
    }
    #modal-track {
        will-change: transform;
        display: flex;
    }
    #miniaturas-track::-webkit-scrollbar {
        display: none;
    }
    .miniatura-btn {
        transition:
            border 0.3s,
            opacity 0.3s;
    }
    .miniatura-btn:hover {
        opacity: 1 !important;
        transform: scale(1.08);
    }
    .boton-galeria {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 10px 28px;
        background: #ebccc4;
        color: white;
        border: none;
        border-radius: 50px;
        font-size: 14px;
        font-weight: 600;
        letter-spacing: 0.2em;
        cursor: pointer;
        transition: all 0.3s ease;
    }
    .boton-galeria:hover {
        background: #4e6070;
        box-shadow: 0 4px 16px rgba(99, 119, 141, 0.4);
        transform: translateY(-2px);
    }
    @keyframes ping {
        75%,
        100% {
            transform: scale(1.6);
            opacity: 0;
        }
    }
    .animate-ping {
        animation: ping 1.5s cubic-bezier(0, 0, 0.2, 1) infinite;
    }
</style>
